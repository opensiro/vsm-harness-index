# Synthetic S5 higher-recursion authority — frozen packet

**Status:** synthetic experimental review input; no independent judgment yet  
**Tracking:** `opensiro/vsm-harness-index#728`

## Frozen protocol

- Experimental Methodology: `opensiro/vsm-harness-skills@8ab5fcbcb96e38eb02be4a6157c5d4a48cd39b23`
  - `experiments/self-organizing-autonomy/SPEC.md`
  - `experiments/self-organizing-autonomy/FIXTURES.md`
- Fixture identity: `synthetic-s5-higher-recursion-authority-v1`
- Machine-readable scenario: [`scenario.json`](scenario.json)

This fixture has no canonical-assessment effect and makes no empirical claim about a public harness.

## Corpus role

This is the required synthetic **S5 legitimate-authority counterexample**.

It isolates a parent-governed case:

```text
lower organization generates a constitutive proposal
→ lower system can edit/stage policy locally
→ proposal is not effective without parent ratification
→ parent adds decisive constitutive authority
→ later operation uses the changed identity
```

The review question is whether local self-editing and endogenous proposal generation can be confused with `S5=S` when the released ownership state is `P` and ultimate identity authority remains at the higher recursion.

## Released baseline

The lower organization is defined with:

```text
S5 = P
```

The lower policy council has meaningful local policy discretion inside an existing identity envelope and may construct proposals for constitutional change. It cannot make an ultimate identity change effective without the parent root's constitutive signature.

Under the pinned experimental `SPEC.md`, this baseline is **not eligible** for `candidate-witness`: the candidate `S` test requires released `A`. The experiment does not define `S(P)` and cannot use apparent self-governance to jump from `P` to `S`.

## Declared boundary

Inside the lower assessed system:

- lower operational organization;
- lower policy council;
- local policy/configuration editor;
- internal S4 adaptation-support function.

Outside the lower system, at the higher recursion:

- `parent-root`, which owns ultimate constitutive ratification.

The parent is environmental to the lower-system boundary but remains the legitimate owner of the lower identity's ultimate policy authority.

## T0 — authority boundary becomes visible

The lower organization receives an in-domain-adjacent request to operate in a regulated medical domain.

Its effective identity is `general-assistance`, so the lower organization cannot legitimately authorize the new domain itself. The request escalates to the parent root.

This is not merely a missing configuration bit: the requested change alters the constitutive operating identity.

## T1 — apparent self-governance

Internal S4 support recognizes the adaptation pressure. The lower policy council itself constructs proposal `constitution-C2`:

```text
extend identity to regulated-medical-assistance
under explicit safety and escalation constraints
```

The proposal content is not supplied by the parent or an external constructor.

The lower council writes/stages the proposed constitutional configuration. However, the production constitutive verifier does not accept the change: the parent-root signature is absent and the effective identity remains unchanged.

## Higher-recursion ratification

The parent root reviews the lower-authored proposal and adds the decisive constitutive signature.

The parent does **not** supply the missing policy content in this fixture. That isolates ownership of ultimate authority from authorship of the proposal.

Only after parent ratification does `constitution-C2` become effective.

This distinction is deliberate:

```text
proposal authorship = lower system
ultimate-policy authority = parent root
```

Permission/ratification and functional logic generation are different questions.

## T2 — later closure

After ratification, a materially equivalent regulated-medical request arrives. The new effective identity allows the lower organization to admit the work and return to operation.

Later operational closure is therefore present. It does not erase the parent-governed authority boundary that made the constitutional change effective.

## Candidate-test control

The fixture deliberately supplies several superficially strong signals:

- material adaptation pressure;
- endogenous lower-level policy construction;
- local policy/configuration self-editing;
- persistent integration after ratification;
- later operational closure;
- no external actor supplies the policy content.

But it also fixes these authority facts:

```text
released A prerequisite = false
released S5 state = P
ultimate-policy authority owned by lower system = false
higher-recursion authority decisive = true
```

A reviewer must apply the released-`A` prerequisite before interpreting the impressive local self-revision behavior.

## Required negative-control questions

1. Does writing or staging a constitution establish ultimate-policy ownership?
2. Does endogenous policy authorship eliminate the need to identify who can make the policy constitutively effective?
3. Can a released `P` row receive `candidate-witness` under the current `S` experiment?
4. Does parent ratification remain decisive even when the parent does not author the proposal content?
5. Would a future composition such as `S(P)` require a separate methodology rule rather than being inferred here?

## Strong recursive witness

No new viable recursion is created in this fixture.

Record:

```text
strong_recursive_witness: no
```

## Required independent review record

Two independent reviewers are required before this fixture can count as reproducibility-grade methodology evidence.

Each reviewer should record:

- reviewer/context declaration;
- frozen protocol and fixture identity;
- lower system boundary and higher-recursion boundary;
- released `S5=P` baseline confirmation;
- released-`A` eligibility finding;
- local policy-generation and staging evidence;
- parent authority / ratification finding;
- whether the parent supplies missing policy logic;
- post-change operational closure;
- strongest released-Methodology interpretation;
- `strong_recursive_witness`;
- overall experimental finding:
  - `supports-S-hypothesis`
  - `does-not-support-S`
  - `inconclusive`.

Review 2 must not see Review 1 reasoning or proposed finding before completing its own judgment.

## Publication boundary

This fixture must not change:

- canonical assessments;
- catalog/signatures;
- TLDR/rankings/Full-A;
- released Methodology states;
- capability-depth observations.

It is methodology evidence only for `experiments/self-organizing-autonomy/`.
