# Self-organizing autonomy `S` — fixture candidate matrix

**Status:** experimental planning artifact; no findings  
**Methodology:** `opensiro/vsm-harness-skills@947b42e77551ed1a86456f8a812c72a96a36506a`  
**Index snapshot used for current corpus planning:** `opensiro/vsm-harness-index@d53c54b2b8164c0d323c0bf10578417eab38af74`

This file selects candidate systems for the fixture classes required by the experimental `S` corpus. Selection is based only on canonical assessments and fixture-design needs and is **not** an experimental `S` judgment.

A listed repository still requires its own frozen packet, pinned primary-evidence review, per-function screen where applicable, two independent judgments, and synthesis before it can satisfy a fixture class.

## Candidate matrix

| Required fixture class | Candidate | Canonical assessment reason for selection | Experimental question to test |
| --- | --- | --- | --- |
| Clear `C` case | `SmileLikeYe/agent-chief` | Canonical `S3*=C` with otherwise narrow `S1=A`: Chief supplies a concrete complementary acceptance/retry/escalation path but standard resident wiring does not supply an autonomous verifier callback as the decisive audit owner. | Does the released-`A` prerequisite prevent a constructor audit path from being treated as `S`, while the independent S1 screen remains separate? |
| Clear `A` case | `truefoundry/trueforge` | Canonical `S1=A` with no credited metasystem functions: the root agent absorbs open-ended task variety using its shipped model/tool/delegation repertoire. | Can novel operational variety be absorbed through existing S1 repertoire without evidence that the operational repertoire itself was reconstructed? |
| Self-modifying non-`S` case | `raphaelchristi/harness-evolver` | Corrected canonical boundary is `A — — C — —` and explicitly treats generated/modified candidate harnesses as separate systems-in-focus. Harness Evolver autonomously changes target harness code, while Critic-added audit configuration still has a constructor closure gap at the pinned ref. | Does self-modification of another system fail the S1 `S` test at the Harness Evolver boundary, without using the ineligible `S3*=C` path as a shortcut? |
| Repertoire-change non-recursive candidate | `annex-ai/scion` | Canonical `S2=A`, `S3=A`, `S4=A`; shipped agent-team mode lets the lead add/change specialist roles and revise dependency/interface organization, while the assessment explicitly does not treat spawned specialists as viable recursions. | Is there a concrete trace where a prior coordination/current-control repertoire is insufficient, Scion endogenously changes team organization, and the new repertoire closes the disturbance without establishing a new viable recursion? |
| Strong recursive candidate | `Xtra-Computing/MegaAgent` | Canonical `S1=A`, `S2=A`, `S3=A`, `S3*=A`; the runtime generates a task-specific organization and agents can recursively recruit subordinates, but the canonical assessment deliberately does not assume nested workers are complete viable recursions. | Can primary evidence establish an endogenous transition from insufficient organization to a newly created lower viable recursion with its own required regulatory relations and parent integration? |
| S3* counterexample | `cbrock84/headcount` | Canonical `S3*=C`: reviewer independence is specified, but the installed user-facing boundary leaves separate reviewer identity/wiring to composition; richer repository dogfood cannot be borrowed across the boundary. | Does apparent audit self-organization fail when complementary independence is not autonomously instantiated/closed at the declared boundary? |
| S5 counterexample | `paperclipai/paperclip` | Canonical `S5=P`: company purpose/strategy authority is explicitly reserved to the board despite autonomous CEO/manager operation elsewhere. | Does apparent organizational self-direction fail `S5=S` because ultimate policy remains legitimately parent-owned rather than endogenously reconstructable by the company? |

## Active fixture status

| Candidate | Fixture status |
| --- | --- |
| `razzant/ouroboros` | Initial real-system fixture; Review 1 withheld, independent Review 2 handoff in #274. |
| `annex-ai/scion` | Packet frozen; Review 1 withheld, independent Review 2 handoff in #290. |
| `raphaelchristi/harness-evolver` | Canonical S3* same-ref correction completed, packet refrozen; Review 1 withheld, independent Review 2 handoff in #300. |
| `truefoundry/trueforge` | Clear-A packet frozen; Review 1 withheld, independent Review 2 handoff in #304. |
| `SmileLikeYe/agent-chief` | Clear-C packet frozen; Review 1 withheld, independent Review 2 handoff in #305. |
| `Xtra-Computing/MegaAgent` | Strong-recursive packet being prepared under #306. |
| `cbrock84/headcount` | Planned S3* counterexample; no fixture packet yet. |
| `paperclipai/paperclip` | Planned S5 counterexample; no fixture packet yet. |

## Alternate controls

These remain useful substitutes or follow-up controls, not primary active selections:

- `humbertobellor/henterprise` — broader constructor control with canonical `S2=C` and `S3*=C`; retained as a follow-up but Chief is cleaner because fewer unrelated positive functions need screening.
- `simular-ai/Agent-S` — another narrow `S1=A` case where reflection remains task-local use of existing repertoire.
- `open-multi-agent/open-multi-agent` — additional constructor control: `S3=C` because OMA ships the recovery/replan seam but not the decisive autonomous replanner.
- `gagans23/agent-os` — parent-governed adaptation control: reusable skills are synthesized from traces but require human approval before installation (`S4=P`).
- `TinyAGI/tinyagi` — boundary control showing that an agentic assembled deployment does not transfer organizational ownership from adjacent terminal-agent CLIs into the first-party repository boundary.

## Order

Continue the corpus in this order after the already-started fixtures:

1. MegaAgent — strong recursive-generation candidate.
2. Headcount — S3* independence counterexample.
3. Paperclip — S5 parent-authority counterexample.
4. Henterprise — optional broader constructor follow-up if Chief does not sufficiently stress the `C` boundary.

Do not promote any row from candidate to fixture finding from this matrix alone.
